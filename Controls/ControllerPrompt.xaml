<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ControllerPlayground.Controls.ControllerPrompt"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ControllerPlayground.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:ControllerPlayground.Controls"
    mc:Ignorable="d">

    <StackPanel
        Orientation="Horizontal"
        Spacing="8">

        <controls:ControllerGlyph
            Family="{x:Bind Family, Mode=OneWay}"
            Button="{x:Bind Button, Mode=OneWay}"/>

        <TextBlock
            x:Name="LabelText"
            Text="{x:Bind Label, Mode=OneWay}"
            FontSize="18"
            VerticalAlignment="Center"/>
    </StackPanel>
    
</UserControl>
